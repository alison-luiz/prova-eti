Para rodar o projeto, primeiro é necessário subir a base de dados (PostgreSQL)

    $ docker compose up -d

#

Inicie o projeto API

    $ npm i
    $ npm run start:dev

#

Inicie o projeto Frontend

    $ npm i
    $ npm run dev
